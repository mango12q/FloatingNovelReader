<UserControl x:Class="FloatingNovelReader.Controls.OverlayControlBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="40" d:DesignWidth="600">
    <Border Background="{StaticResource ControlBarBrush}" CornerRadius="0" Padding="8,4">
        <DockPanel>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <!-- ≡ 菜单按钮：弹出章节目录 / 书签列表 -->
                <Button x:Name="MenuBtn" Content="≡" FontSize="16"
                        Foreground="White" Background="Transparent" BorderThickness="0"
                        Padding="8,2" Margin="2,0" Cursor="Hand" ToolTip="菜单"
                        AutomationProperties.Name="菜单按钮"
                        AutomationProperties.HelpText="打开章节目录和书签列表"
                        Click="OnMenuClick">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                    CornerRadius="3" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                                  TextElement.Foreground="{TemplateBinding Foreground}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{StaticResource ControlBarHoverBrush}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{StaticResource ControlBarPressedBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                    <Button.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="章节目录" Click="OnMenuChapterList"/>
                            <MenuItem Header="添加书签" Click="OnMenuAddBookmark"/>
                            <MenuItem Header="书签列表" Click="OnMenuBookmarkList"/>
                            <Separator/>
                            <MenuItem Header="设置..." Click="OnMenuSettings"/>
                        </ContextMenu>
                    </Button.ContextMenu>
                </Button>
                <TextBlock x:Name="BookTitleText" Text="" Foreground="White" FontSize="13" VerticalAlignment="Center" Margin="6,0"/>
                <TextBlock Text="  " Foreground="White" FontSize="13" VerticalAlignment="Center"/>
                <TextBlock x:Name="ChapterText" Text="" Foreground="White" FontSize="13" VerticalAlignment="Center" Opacity="0.85"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                <!-- Button 控件：默认 Click 事件；Button 内部对 PreviewMouseLeftButtonDown 设置 handled=true，
                     冒泡到 RootBorder 时 handled=true，DragMove 不会启动，Button.Click 正常触发 -->
                <Button x:Name="SettingsBtn" Content="⚙" FontSize="16"
                        Foreground="White" Background="Transparent" BorderThickness="0"
                        Padding="10,2" Margin="2,0" Cursor="Hand" ToolTip="设置"
                        AutomationProperties.Name="设置按钮"
                        AutomationProperties.HelpText="打开阅读器设置"
                        Click="OnSettingsClick">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                    CornerRadius="3" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                                  TextElement.Foreground="{TemplateBinding Foreground}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{StaticResource ControlBarHoverBrush}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{StaticResource ControlBarPressedBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button x:Name="CloseBtn" Content="×" FontSize="16"
                        Foreground="White" Background="Transparent" BorderThickness="0"
                        Padding="10,2" Margin="2,0" Cursor="Hand" ToolTip="关闭"
                        AutomationProperties.Name="关闭按钮"
                        AutomationProperties.HelpText="关闭阅读窗口"
                        Click="OnCloseClick">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                    CornerRadius="3" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                                  TextElement.Foreground="{TemplateBinding Foreground}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{StaticResource ControlBarHoverBrush}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{StaticResource ControlBarPressedBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </DockPanel>
    </Border>
</UserControl>
