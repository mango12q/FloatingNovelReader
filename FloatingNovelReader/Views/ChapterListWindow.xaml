<Window x:Class="FloatingNovelReader.Views.ChapterListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="章节目录"
        Width="500" Height="700"
        MinWidth="400" MinHeight="500"
        WindowStartupLocation="CenterOwner"
        Background="White"
        FontSize="{x:Static SystemFonts.MessageFontSize}"
        AutomationProperties.Name="章节目录"
        AutomationProperties.HelpText="浏览书籍目录，点击章节跳转阅读">
    <Grid Margin="6">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="BookTitleText" FontSize="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=FontSize}" FontWeight="Bold" Margin="6"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="VolumeList"
                          AutomationProperties.Name="章节目录列表"
                          AutomationProperties.HelpText="按卷分组的章节列表，点击章节跳转阅读">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Expander Header="{Binding Title}" IsExpanded="True" Margin="4">
                            <ItemsControl ItemsSource="{Binding Chapters}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Title}" Padding="6,4" Cursor="Hand"
                                                   MouseLeftButtonUp="OnChapterClick" Tag="{Binding}"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Expander>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Window>
